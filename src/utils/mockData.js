export const restList = [
   //dummy data
  ];

//   console.log("size---", restList.length);
//   for (const key in restList) {
//       if (Object.hasOwnProperty.call(restList, key)) {
//           const element = restList[key];
//           // console.log("name", element.card.card.info.name);
//       }
//   }
